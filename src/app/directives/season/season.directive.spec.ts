import { SeasonDirective } from './season.directive';

describe('SeasonDirective', () => {
  it('should create an instance', () => {
    const directive = new SeasonDirective();
    expect(directive).toBeTruthy();
  });
});
